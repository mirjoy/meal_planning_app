<%= form_for(:banned_food, url: banned_foods_path, method: :post, remote: true) do |f| %>
	<%= f.label :name, "Food to Exclude (ex: banana)" %>
	<%= f.text_field :name %>
	<%= f.submit "Submit", class: "btn btn-default" %>
<% end %>